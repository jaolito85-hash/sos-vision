{
  "name": "sos-vision-mobile",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "App nativo (Capacitor) da camada B — rastreamento em background para vulneráveis cadastrados.",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "sync": "cap sync",
    "add:android": "cap add android",
    "open:android": "cap open android",
    "run:android": "cap run android"
  },
  "dependencies": {
    "@capacitor-community/background-geolocation": "^1.2.23",
    "@capacitor/android": "^6.2.0",
    "@capacitor/core": "^6.2.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.2.0",
    "typescript": "^5.6.3",
    "vite": "^5.4.11"
  }
}
